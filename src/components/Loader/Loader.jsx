import { StyledLoader, StyledOverlay } from './Loader.styled';

const Loader = () => {
    return (
        <StyledOverlay>
            <StyledLoader />
        </StyledOverlay>
    );
};

export default Loader;
